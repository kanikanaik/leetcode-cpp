#include <iostream>
#include <bits/stdc++.h>
using namespace std;

int main()
{
    vector<int> nums = {6, 3, 6};
    int i, j, k;
    nums[0] = i;
    nums[1] = j;
    nums[2] = k;

    int n = nums.size();

    int trip = 0;

    if (0 < i < j < k < n)
    {
        if (nums[i] == nums[j] * 2 && nums[k] == nums[j] * 2)
        {
                }
    }
    else
    {
        cout << trip;
    }
}