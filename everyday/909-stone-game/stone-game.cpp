class Solution {
public:
    bool stoneGame(vector<int>& piles) {
        // int alice = 0;
        // int bob = 0;
        // for(int i = 0 ; i < piles.size(); i++){
        //     if(i % 2 == 0){
        //         alice += piles[i];
        //     }else{
        //         bob += piles[i];
        //     }
        // }

        // if(alice > bob){return true;}else{return false;}
        return true;
    }
};